import type { KQLQuery } from '@kql-ts/core';
export interface KqlRequest {
    query: KQLQuery;
    endpoint: string;
    fetch: typeof globalThis.fetch;
    language?: string;
    auth?: `Bearer ${string}` | `Basic ${string}`;
    headers?: HeadersInit;
    timeout?: number;
}
export declare function kqlHandler({ query, endpoint, language, fetch, auth, headers, timeout }: KqlRequest): Promise<any>;
