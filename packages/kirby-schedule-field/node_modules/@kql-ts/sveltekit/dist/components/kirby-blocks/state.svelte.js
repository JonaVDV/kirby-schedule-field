import { getContext, setContext } from 'svelte';
export class KirbyBlocks {
    blocks = $state.raw({});
    constructor(blocks) {
        this.blocks = blocks;
    }
    setBlocks(blocks) {
        this.blocks = blocks;
    }
    getAllBlocks() {
        // get all the keys together with the name of the component
        return Object.keys(this.blocks);
    }
    getBlock(name) {
        return this.blocks[name];
    }
}
const KEY = Symbol('kirby-blocks');
export function setBlocksContext(blocks) {
    return setContext(KEY, new KirbyBlocks(blocks));
}
export function getBlocksContext() {
    return getContext(KEY);
}
